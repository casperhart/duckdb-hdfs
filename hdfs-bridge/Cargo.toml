[package]
name = "hdfs-bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
hdfs-native = { git = "https://github.com/casperhart/hdfs-native", branch = "kms" }
libc = "0.2"
